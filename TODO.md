# TODO

- [ ] Lens presets shipped as data (focus + characteristic question + declared blind spot) rather
      than improvised per run: security, maintenance, cost, 3am-operator.
- [ ] Cross-pollination round: hand each peer only the *other's best idea*, stripped of attribution,
      and ask what it unlocks — instead of relaying the whole turn.
- [ ] `resume` — reopen a saved transcript and keep going in a later session.
- [ ] Auto-save transcripts behind a flag, instead of only on request.
- [ ] Idea provenance is still hand-tracked by the host. The peer's committed top-3 catches ranking
      drift, but nothing stops a `[both]` idea being re-tagged `[host]` in the harvest.
- [ ] Measure it: do `[both]` ideas actually show up in what gets built, or does the host's original
      position win most of the time anyway?
